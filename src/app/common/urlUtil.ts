import { environment } from "../environments/environment";

export class UrlUtils {
public static albums = environment.host + '/albums';
public static musicalGenres = environment.host + '/musical-genres';
}

